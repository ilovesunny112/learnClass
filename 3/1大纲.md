




# 大纲
- 面向对象深入解析
	+ 函数的三种角色：函数也是对象
	+ Function 
	+ Functiion.prototype:call/apply/bind
	+ 面试题讲解
- call && apply
   + 获取数组最大值最小值
   + eval及括号表达式
   + 获取平均数
	     + 类数组转换数组
	     + try,catch,finally
	     + 浏览器重用的报错信息 
- 综合案例
	+ ajax获取数据以及数据绑定
	+ 前后端协作模型
	+ ajax获取数据四步操作
	+ json
	+ es6模板字符串做数据绑定 
	+ 实现表格排序
	     + sort原理
	     + dom映射机制
	     + 单列升序
	     + 单列升降序切换
	     + 多列升降序切换